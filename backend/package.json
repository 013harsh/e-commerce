{
  "name": "ecommerce-backend",
  "version": "1.0.0",
  "description": "E-commerce JSON Server backend",
  "type": "module",
  "scripts": {
    "server": "json-server --watch db.json --port 3000",
    "start": "json-server --watch db.json --port 3000"
  },
  "dependencies": {
    "json-server": "^1.0.0-beta.12"
  }
}
